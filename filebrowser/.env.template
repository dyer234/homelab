TZ=America/Los_Angeles
DOMAIN=dyerwolf.xyz

# Run as this uid:gid so uploaded files match the ARR stack (PUID/PGID 1000)
PUID=1000
PGID=1000

# Host path to the media library that File Browser will serve (read-write).
# Matches the media-streaming MEDIA_HOST so the same library is browsable.
MEDIA_HOST=/mnt/media1/torrents
